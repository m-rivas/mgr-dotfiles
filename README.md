# Dotfile Configuration #

These dotfiles are meant to be used with the GNU stow command. After pulling this directory down please run the following to install these configs on your system:
> `stow --adopt [config name]` Where "config name" is a configuration you want to stow, i.e. nvim, kitty, etc.
